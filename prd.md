# 📄 Product Requirement Document (PRD) — Receipt Web Application

> **ระบบออกใบเสร็จรับเงินออนไลน์ (Online Receipt & Billing Management System)**  
> **บริษัท ศรีสุข พูนทรัพย์ ยางพารา จำกัด**  
> **เวอร์ชันเอกสาร:** 2.5  
> **วันที่อัปเดตล่าสุด:** 17 สิงหาคม 2569 (2026-08-17)

---

## 1. 🎯 วัตถุประสงค์ของโปรเจกต์ (Project Overview & Objectives)

ระบบออกใบเสร็จรับเงินและระบบใบสำคัญจ่ายออนไลน์พัฒนาขึ้นเพื่อทดแทนการเขียนเอกสารด้วยมือ เพิ่มประสิทธิภาพการทำงานของพนักงาน ลดข้อผิดพลาดในการคำนวณเงินสด/ส่วนลด/DRC และจัดเก็บข้อมูลอย่างเป็นระบบลงใน **Google Sheets** แบบเรียลไทม์ โดยรองรับการพิมพ์ใบเสร็จและใบสำคัญจ่ายมาตรฐาน A4 ได้ถูกต้อง สวยงาม และน่าเชื่อถือ

---

## 2. 🏛️ โครงสร้างสถาปัตยกรรมระบบ (System Architecture)

```
[ Frontend: React + Vite + Tailwind ]
(มี Sidebar สลับหน้าใบเสร็จรับเงิน และ ใบสำคัญจ่าย)
               │
               ▼ (HTTP POST / GET)
[ Cloudflare Worker Proxy (CORS Bypass & Security) ]
               │
               ▼ (JSON Payload แบ่ง Route ด้วยตัวแปร action)
[ Google Apps Script (Main.gs, Auth.gs, Receipt.gs, Utils.gs) ]
               │
               ▼ (Append & Query Rows)
[ Google Sheets Database (ชีตใบเสร็จ / ชีตใบสำคัญจ่าย / ชีต Config) ]
```

1. **Frontend:** พัฒนาด้วย React, Vite, Vanilla CSS / Tailwind CSS รันบนสภาพแวดล้อมระบบเว็บแอปพลิเคชัน
2. **Proxy Layer:** Cloudflare Worker Proxy สำหรับจัดการ CORS Headers และเพิ่มความปลอดภัยในการสื่อสาร
3. **Backend API:** Google Apps Script ทำหน้าที่เป็น REST API (JSON) อ่านและเขียนข้อมูล (ปัจจุบันแยกย่อยเป็น `Main.gs`, `Auth.gs`, `Receipt.gs`, `Utils.gs`)
4. **Database:** Google Sheets

---

## 3. 📊 โครงสร้างฐานข้อมูล: ใบเสร็จรับเงิน (Receipt Database - 20 คอลัมน์)

| คอลัมน์ | ชื่อหัวข้อ (Header) | คำอธิบาย & ฟิลด์ที่รับจาก Frontend |
|:---:|:---|:---|
| **Col A (1)** | วันที่ | วันที่ออกใบเสร็จ (`docDateVal`) |
| **Col B (2)** | เลขที่ใบเสร็จ | เลขที่เอกสารอัตโนมัติ (`receiptNo`) |
| **Col C (3)** | นามผู้ซื้อ | ชื่อลูกค้า/บริษัทคู่ค้า (`buyerName`) |
| **Col D (4)** | ที่อยู่ | ที่อยู่จัดส่ง/ออกเอกสาร (`buyerAddress`) |
| **Col E (5)** | เลขประจำตัวผู้เสียภาษี | เลข 13 หลัก (`buyerTaxId`) |
| **Col F (6)** | งวด | งวดประจำเดือน/ปี (`period`) |
| **Col G (7)** | รายการสินค้าหรือบริการ | ชื่อหมวดสินค้า (`itemTitle`) |
| **Col H (8)** | จำนวน | จำนวนสินค้า (`quantity`) |
| **Col I (9)** | ราคาต่อหน่วย | ราคาต่อหน่วย บาท (`unitPrice`) |
| **Col J (10)** | DRC(%) | เปอร์เซ็นต์เนื้อยาง (`drc`) |
| **Col K (11)** | **เพิ่มลด** | ยอดเงินรายการปรับลด/ส่วนลด (`discountAmount`) |
| **Col L (12)** | **รายละเอียด** | รายละเอียดเพิ่มเติม/หมายเหตุ (`discountDetails`) |
| **Col M (13)** | **จำนวนเงิน** | **ยอดเงินสุทธิหลังหักรายการปรับลด** (`amount`) |
| **Col N (14)** | ชำระโดย | วิธีการชำระเงิน (`paymentFull`) |
| **Col O (15)** | วันที่โอน/สั่งจ่าย | วันที่ชำระเงิน (`payDateVal`) |
| **Col P (16)** | หมายเหตุ | หมายเหตุเพิ่มเติม (`notes`) |
| **Col Q (17)** | ผู้รับเงิน | ชื่อพนักงานผู้รับเงิน (`cashierName`) |
| **Col R (18)** | สถานะ | สถานะเอกสาร (`status`) |
| **Col S (19)** | สาเหตุที่ยกเลิก | ข้อความระบุสาเหตุยกเลิก (`cancelReason`) |
| **Col T (20)** | วันที่พิมพ์/บันทึก | Time Stamp บันทึก/พิมพ์ (`printedTimestamp`) |

---

## 4. 📊 โครงสร้างฐานข้อมูล: ใบสำคัญจ่าย (Payment Voucher - 16 คอลัมน์)

ใช้หลักการบันทึก **"1 รายการย่อย = 1 แถว"** เพื่อรองรับใบสำคัญจ่ายที่มีหลายรายการ:

| คอลัมน์ | ชื่อหัวข้อ | คำอธิบาย |
|:---:|:---|:---|
| **A (1)** | วันที่ทำรายการ | วันเวลาที่กดบันทึกข้อมูล (Timestamp) |
| **B (2)** | เลขที่เอกสาร | รันอัตโนมัติจากฝั่ง Backend (เช่น `PV69080001`) |
| **C (3)** | จ่ายให้ (Receiver) | ดึงจาก Dropdown หรือพิมพ์ใหม่ (Auto-save) |
| **D (4)** | คำอธิบายรวม | คำอธิบายภาพรวมของเอกสาร (ช่องด้านบน) |
| **E (5)** | วันที่เอกสาร (Doc date) | วันที่เอกสารหลัก |
| **F (6)** | เลขที่เอกสารอ้างอิง | บิล/ใบแจ้งหนี้อ้างอิง |
| **G (7)** | **วันที่รายการย่อย** | วันที่เอกสารของแต่ละบรรทัดในตาราง |
| **H (8)** | **รายการ (Description)** | รายละเอียดปลีกย่อยในตาราง |
| **I (9)** | **จำนวนเงิน (Amount)** | ยอดเงินของรายการย่อยนั้นๆ |
| **J (10)** | ชำระโดย | วิธีการชำระเงิน (ด้านล่าง) |
| **K (11)** | เลขที่เช็ค | เลขที่เช็ค (ถ้ามี) |
| **L (12)** | ธนาคาร | ธนาคารที่ใช้โอน/จ่าย (ด้านล่าง) |
| **M (13)** | วันที่ชำระเงิน | วันที่ทำการชำระเงิน (ด้านล่าง) |
| **N (14)** | หมายเหตุ | หมายเหตุเพิ่มเติม |
| **O (15)** | ผู้จัดทำ | ชื่อพนักงานที่เข้าสู่ระบบ (Cashier) |
| **P (16)** | สถานะ | `ปกติ` หรือ `ยกเลิก` |

**หมายเหตุระบบ Config สำหรับใบสำคัญจ่าย:** จะใช้ชีต Config ตัวเดิมร่วมกับใบเสร็จ แต่ทำการเพิ่มแท็บ (Sheet) เข้าไป 3 แท็บ ได้แก่ `Voucher_Receiver` (เก็บชื่อผู้รับเงิน), `Voucher_PaymentMethod` (เก็บวิธีชำระเงิน), และ `Voucher_BankAcc` (เก็บบัญชีธนาคารสำหรับจ่ายเงิน)

---

## 5. 🖨️ เครื่องมือพิมพ์เอกสาร A4 (Print Engine)

### 5.1 ใบเสร็จรับเงิน (Receipt):
- ขนาดหน้ากระดาษ A4 พิมพ์ออกเป็น 2 ชุดอัตโนมัติ (ต้นฉบับ/สำเนา) ในคำสั่งเดียว
- มีระบบคำนวณความจุรายการแบบไดนามิก รองรับสูงสุด 9-10 รายการต่อหน้า พร้อมล็อคตำแหน่งกรอบด้านล่างไม่ให้เลื่อนตกขอบ
### 5.2 ใบสำคัญจ่าย (Payment Voucher):
- ขนาดหน้ากระดาษ A4 พิมพ์ออกเต็มหน้า 1 ใบ โดยมีช่องเซ็นชื่อผู้อนุมัติ/ผู้จัดทำด้านล่าง
- รองรับลายน้ำ "ยกเลิก" สำหรับเอกสารที่ถูกยกเลิกสถานะ
- ระบบคำนวณและแปลงยอดเงินรวมเป็นคำอ่านภาษาไทย (เช่น "หนึ่งพันบาทถ้วน") โดยอัตโนมัติ

---

## 6. 📜 ประวัติการอัปเดตเวอร์ชัน (Release Version History)

| เวอร์ชัน | วันที่อัปเดต | รายละเอียดการเปลี่ยนแปลง |
|:---:|:---:|:---|
| **v1.0** | 2026-08-09 | พัฒนาระบบออกใบเสร็จรับเงินพื้นฐาน ฟอร์มกรอกข้อมูล และการเชื่อมต่อ Google Sheets |
| **v1.7** | 2026-08-12 | แก้ไขสูตร Col M ให้หักลบปรับลด ฟิกซ์การแปลง Time Stamp วันที่ไทย และจัดสไตล์ท้ายกระดาษ A4 |
| **v2.3** | 2026-08-12 | ปรับตั้งค่าความจุรายการต่อหน้าพิมพ์ A4 ล่าสุด: รายการปกติ (1 บรรทัด) ได้ **9–10 รายการ** |
| **v2.4** | 2026-08-14 | แก้ไขระบบ Login ให้เชื่อมต่อผ่าน Cloudflare Worker, เพิ่มฟังก์ชันแสดงชื่อผู้รับเงิน (Cashier) |
| **v2.5** | 2026-08-17 | **เตรียมโครงสร้างใบสำคัญจ่าย:** วางโครงสร้างตารางข้อมูลใบสำคัญจ่าย 16 คอลัมน์ และทำการ Refactor แยกไฟล์ `Code.gs` ออกเป็น 4 ไฟล์ย่อย (`Main.gs`, `Auth.gs`, `Receipt.gs`, `Utils.gs`) เพื่อเตรียมความพร้อมของระบบ Backend |
