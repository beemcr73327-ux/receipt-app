# วิเคราะห์สถาปัตยกรรมเว็บแอพออกบิล + ระบบซื้อขาย Lot (ธุรกิจยางพารา)

## 1. สรุปภาพรวม

โครงสร้างปัจจุบัน (ออกบิล):

```
Internet → Cloudflare → Worker Backend
                              ├── Authentication
                              ├── Business Logic
                              └── Validation
                                    ├── Receipt
                                    ├── PV
                                    └── Audit Log
                                          → Database (D1/SQL)
                                                ├── Backup
                                                └── Reporting → Google Sheets
```

**ระดับปัจจุบัน:** เหมาะสมสำหรับ MVP / ธุรกิจขนาดเล็ก-กลาง (SME) โดยใช้แนวทาง serverless-at-the-edge (Cloudflare Workers + D1) ซึ่งเป็นสถาปัตยกรรมที่ทีมมืออาชีพสมัยใหม่ก็เลือกใช้จริง เนื่องจากเร็ว ต้นทุนต่ำ ดูแลง่าย

**ข้อจำกัด:** ยังขาดกลไกที่จำเป็นสำหรับระบบการเงิน (ทั้งการออกบิล และระบบซื้อขาย Lot ที่กำลังจะเพิ่ม) ซึ่งมีผลทางกฎหมายและบัญชีโดยตรง ความผิดพลาดในระบบนี้ไม่ใช่แค่บั๊กซอฟต์แวร์ แต่กระทบการปฏิบัติตามกฎหมายภาษีและความน่าเชื่อถือทางบัญชี

**ข้อสรุปเชิงกลยุทธ์:** ไม่ต้องรื้อโครงสร้างเดิมทั้งหมด แต่ต้องเติม "3 กลไกหลัก" (รายละเอียดในหัวข้อ 3) ลงในโครงสร้างเดิมก่อน แล้วจึงต่อยอดฟีเจอร์ซื้อขาย Lot/สต็อกบนกลไกเดียวกันนี้ เพราะทั้งสองฟีเจอร์ต้องพึ่งกลไกชุดเดียวกัน

---

## 2. รายละเอียดจุดที่ควรปรับปรุงในระบบออกบิล

### 2.1 Authentication & Authorization
- ใช้ JWT แบบ short-lived คู่กับ refresh token
- ใช้ RBAC แยกสิทธิ์ เช่น พนักงานขาย / ผู้จัดการ / ผู้ดูแลระบบ
- จำกัดจำนวนครั้งการ login ผิด (rate limit เฉพาะ endpoint login)
- เก็บ session/token revocation list เผื่อกรณีต้องยกเลิกสิทธิ์ทันที

### 2.2 Idempotency ในการออกบิล
- ใช้ idempotency key ต่อ 1 การทำรายการ ป้องกันบิลซ้ำเมื่อเน็ตหลุดแล้วกดซ้ำ

### 2.3 เลขที่บิล/ใบกำกับภาษีต้องเรียงต่อเนื่อง
- ต้องออกเลขจาก sequence/counter แบบ atomic ฝั่งเซิร์ฟเวอร์ ห้ามคำนวณจากความยาว list หรือเวลา

### 2.4 Audit Log แบบแก้ไขไม่ได้ (Immutable)
- Insert-only, ห้าม update/delete จากแอปพลิเคชัน
- ใช้ hash-chaining เพื่อพิสูจน์ว่าไม่มีการแก้ไขย้อนหลัง

### 2.5 Backup จริง vs Reporting
- Google Sheets = ช่องทางรายงานให้คนอ่านง่ายเท่านั้น ไม่ใช่ backup จริง
- Backup จริงให้ export ไป Cloudflare R2 แบบ point-in-time อัตโนมัติ + ทดสอบกู้คืนจริงเป็นระยะ

### 2.6 Monitoring & Alerting
- ต่อ error tracking, แจ้งเตือนเมื่อมี login ผิดซ้ำผิดปกติ, เก็บ log ผ่าน Cloudflare Logpush

### 2.7 การจัดการ Secrets
- ใช้ Cloudflare Workers Secrets ห้าม hardcode ค่าลับ

### 2.8 การปฏิบัติตามกฎหมายภาษีไทย
- ตรวจสอบเกณฑ์ e-Tax Invoice ของกรมสรรพากร, VAT, การเก็บเอกสารย้อนหลังตามกฎหมาย
- ปรึกษาผู้ทำบัญชี/สำนักงานบัญชีคู่ขนานกับการพัฒนาระบบ

---

## 3. สามกลไกหลักที่ต้องทำ "ก่อน" เพิ่มฟีเจอร์ใดๆ ต่อ

ทั้งระบบออกบิลและระบบซื้อขาย Lot ต้องพึ่งกลไกชุดเดียวกันนี้ จึงควรทำให้เสร็จในโครงสร้างเดิมก่อน แล้วฟีเจอร์ใหม่ทุกตัวค่อยสร้างต่อบนกลไกนี้ ไม่ต้องทำซ้ำ

### 3.1 Atomic Sequence (เลขที่แบบอะตอมมิก)
**ปัญหาที่ป้องกัน:** เลขที่บิล/เลขที่ใบชั่ง/เลขที่ Lot ซ้ำกันหรือกระโดดข้าม เมื่อมีหลายคนหรือหลายสาขาทำรายการพร้อมกัน
**หลักการ:** การออกเลขต้องเกิดขึ้นที่เซิร์ฟเวอร์เท่านั้น ผ่านกระบวนการที่รับประกันว่าเลขเดียวกันจะไม่มีวันถูกออกซ้ำสอง แม้มี request เข้ามาพร้อมกันหลายสิบครั้งในเวลาเดียวกัน
**ใช้กับ:** เลขที่ใบกำกับภาษี, เลขที่ใบชั่งซื้อ, เลขที่บิลขาย/Lot

### 3.2 Transaction + Lock (การล็อกและทำงานแบบทั้งหมดหรือไม่ทำเลย)
**ปัญหาที่ป้องกัน:** ข้อมูลชุดเดียวกันถูกใช้ซ้ำซ้อนโดยผู้ใช้หลายคนพร้อมกัน (เช่น น้ำยางล็อตเดียวกันถูกจัดเข้าสองบิลขายพร้อมกัน หรือของถูกขายเกินสต็อกที่มีจริง)
**หลักการ:** การกระทำที่กระทบข้อมูลสำคัญ (สร้างบิล, จัดกลุ่ม Lot, ตัดสต็อก) ต้องตรวจสอบเงื่อนไขและบันทึกผลในจังหวะเดียวกันแบบแยกจากกันไม่ได้ (all-or-nothing) และต้องมีข้อจำกัดระดับฐานข้อมูลที่ป้องกันการใช้ทรัพยากรเดียวกันซ้ำ ไม่ใช่พึ่งการตรวจสอบฝั่งหน้าจอเพียงอย่างเดียว
**ใช้กับ:** การจัดกลุ่มรายการซื้อเข้า Lot, การยืนยันสร้างบิลขาย, การตัดสต็อก

### 3.3 Audit Log แบบ Insert-only
**ปัญหาที่ป้องกัน:** ข้อมูลการเงินถูกเขียนทับโดยไม่มีร่องรอย ทำให้ตรวจสอบย้อนหลังหรือแก้ไขข้อผิดพลาดไม่ได้
**หลักการ:** ทุกการเปลี่ยนแปลงที่กระทบตัวเลขทางการเงิน (ยอดขาย, ผลชั่งจากโรงงาน, การปิด Lot) ต้องถูกบันทึกเป็นรายการใหม่เสมอ ไม่เขียนทับของเดิม หากต้องแก้ไขให้สร้าง "รายการปรับปรุง" ที่อ้างอิงของเดิมแทน
**ใช้กับ:** การปิด Lot, การแก้ไขผลชั่ง/ราคาขายจากโรงงาน, การแก้ไขรายการซื้อ

---

## 4. โครงสร้างที่แนะนำ (ปรับปรุงแล้ว)

```
Internet
   │
   ▼
[ Cloudflare Edge ]
   ├── WAF + DDoS protection
   └── Rate limiting + bot management
   │
   ▼
[ Worker Backend (Application layer) ]
   ├── Auth: JWT + RBAC
   ├── Validation: schema check
   ├── Business logic
   ├── Idempotency check
   └── Audit log: immutable, hash-chained
   │
   ▼
[ Data Layer ]
   ├── D1 / SQL (source of truth)
   └── Atomic sequence (ใช้ร่วมกันทั้งเลขบิลและเลข Lot)
   │
   ├──▼ R2 backup (point-in-time, ของจริง)
   └──▼ Google Sheets (reporting only, ไม่ใช่ backup)

[ Monitoring & Alerting ] — เชื่อมต่อจาก Worker Backend ตลอดเวลา
```

---

## 5. การวิเคราะห์ระบบซื้อขาย Lot / สต็อกสินค้า

### 5.1 ภาพรวม flow ที่ออกแบบไว้

```
บันทึกซื้อ (Buy)
   → รายการซื้อทั้งหมด / เลือกมารวมกลุ่ม (Records)
       → สร้างบิลขาย → เข้าคิว "รอผลโรงงาน" (Sell)
           → กรอกผลจริงจากโรงงาน (น้ำหนัก/DRC/ราคา) → ปิด Lot (Factory)
               → คำนวณกำไร-ขาดทุน → สรุปภาพรวม (Dashboard)
```

**จุดแข็งของ flow นี้:**
- แยกสถานะ "ยังไม่จัด Lot" → "รอผลโรงงาน" → "ปิด Lot แล้ว" ชัดเจน ตรงกับ state machine จริงของธุรกิจ
- รองรับการเลือกซื้อข้ามวัน ข้ามสาขา มารวมเป็น Lot เดียวได้ ยืดหยุ่นตรงกับงานจริง
- แยกหน้าคีย์ข้อมูล (Buy) ออกจากหน้าตัดสินใจ (Records/Sell) ทำให้พนักงานหน้างานคีย์ได้เร็ว

### 5.2 ความเสี่ยงที่ต้องแก้ก่อนต่อ backend จริง (mapping กับหัวข้อ 3)

| ความเสี่ยงที่พบใน UI ต้นแบบ | กลไกที่ต้องใช้แก้ |
|---|---|
| เลขที่ใบชั่ง/เลขที่บิลขาย คำนวณจากความยาว list ฝั่งหน้าจอ ซ้ำ/กระโดดได้เมื่อมีหลายสาขาใช้พร้อมกัน | 3.1 Atomic Sequence |
| การเลือกรายการซื้อมารวม Lot ตรวจสอบแค่ฝั่งหน้าจอ สองคนอาจเลือกรายการซื้อชุดเดียวกันไปสร้างคนละ Lot พร้อมกัน (ขายซ้ำ) | 3.2 Transaction + Lock |
| ปุ่มยืนยันสร้างบิลขาย ไม่มีการกันกดซ้ำ/กันส่งซ้ำ | 3.2 Transaction + Lock (ร่วมกับ idempotency key) |
| การ "ปิด Lot" เขียนทับผลชั่ง/ราคา/สถานะตรงๆ ไม่มีร่องรอยของเดิม | 3.3 Audit Log |
| รายการซื้อที่ถูกจัดเข้า Lot แล้ว ไม่มีการล็อกไม่ให้แก้น้ำหนัก/ราคาย้อนหลัง | 3.2 Transaction + Lock |
| ตัวเลขเงิน/น้ำหนักคำนวณด้วย floating point ธรรมดา เสี่ยงปัดเศษสะสมผิดในรายงานกำไร-ขาดทุน | ควรกำหนดมาตรฐานการเก็บตัวเลขการเงินให้แม่นยำ (เช่น หน่วยสตางค์เป็นจำนวนเต็ม) ตั้งแต่ออกแบบ schema |

### 5.3 แนวคิดโครงสร้างข้อมูล (concept level)

ไม่ลงรายละเอียดเชิงเทคนิค แต่แนวคิดคือต้องมีหน่วยข้อมูลหลักดังนี้ และความสัมพันธ์ระหว่างกันต้องบังคับด้วยกฎของฐานข้อมูลเอง ไม่ใช่พึ่งตรรกะฝั่งแอปพลิเคชันอย่างเดียว:

- **รายการซื้อ (purchases):** ต้องมีสถานะบอกว่า "ยังว่าง" หรือ "ถูกจัดเข้า Lot แล้ว" และเมื่อถูกจัดเข้า Lot แล้วต้องแก้ไขค่าน้ำหนัก/ราคาไม่ได้อีก
- **Lot (lots):** เก็บสถานะ (รอผลโรงงาน / ปิดแล้ว), ต้นทุนรวม, และเมื่อ "ปิดแล้ว" ต้องเปลี่ยนสถานะเป็นอ่านอย่างเดียว
- **ความสัมพันธ์รายการซื้อ-Lot:** รายการซื้อ 1 รายการ ต้องผูกกับ Lot ได้สูงสุด 1 Lot เท่านั้น — บังคับด้วยข้อจำกัดของฐานข้อมูล ไม่ใช่แค่การเช็คเงื่อนไขในโค้ด
- **ตัวนับเลขที่ (sequence):** ใช้ร่วมกันได้ทั้งเลขที่ใบชั่งซื้อและเลขที่บิลขาย โดยแยกกลุ่มนับตามประเภทเอกสาร
- **บันทึกประวัติ (audit log):** ทุกการเปลี่ยนสถานะสำคัญ (สร้างรายการซื้อ, จัดเข้า Lot, สร้างบิลขาย, ปิด Lot, แก้ไขผลโรงงาน) ต้องถูกบันทึกเป็นเหตุการณ์ใหม่เสมอ

---

## 6. แผนขั้นตอนทำงานเป็นเฟส (Roadmap)

### เฟส 0 — เติมกลไกหลักลงในโครงสร้างเดิม (ก่อนเพิ่มฟีเจอร์ใดๆ)
- ทำ Atomic Sequence ใน D1 (ใช้ร่วมกันได้ทั้งเลขบิลออกและเลขที่ในระบบ Lot ภายหลัง)
- ทำ Transaction + Lock pattern สำหรับการกระทำที่กระทบข้อมูลการเงิน
- ทำ Audit Log แบบ insert-only
- ผลลัพธ์: ระบบออกบิลปัจจุบันปลอดภัยขึ้นทันที และเป็นฐานให้ฟีเจอร์ใหม่ต่อยอดได้โดยไม่ต้องแก้ซ้ำ

### เฟส 1 — ต่อยอดฟีเจอร์ซื้อขาย Lot / สต็อกสินค้า
- นำ 3 กลไกจากเฟส 0 มาใช้กับ: การจัดกลุ่มรายการซื้อเข้า Lot, การสร้างบิลขาย, การปิด Lot
- ออกแบบโครงสร้างข้อมูลตามหัวข้อ 5.3 พร้อมข้อจำกัดระดับฐานข้อมูลที่ป้องกันการใช้รายการซื้อซ้ำ
- ทดสอบกรณีผู้ใช้หลายคน/หลายสาขาทำรายการพร้อมกัน (concurrency test) ก่อนใช้งานจริง

### เฟส 2 — เสริมความเป็นมืออาชีพส่วนที่เหลือ
- RBAC ละเอียดตามบทบาทจริงของแต่ละสาขา
- Monitoring & Alerting (Sentry, Cloudflare Logpush)
- Backup อัตโนมัติไป R2 แยกจาก Google Sheets + ทดสอบ restore drill
- Secrets management ผ่าน Cloudflare Workers Secrets
- ตรวจสอบความสอดคล้องกับกฎหมายภาษี (e-Tax Invoice, VAT) ร่วมกับผู้ทำบัญชี

---

## 7. Checklist สำหรับนำไปทำงานต่อ

- [ ] สร้าง Atomic Sequence ใน D1 (ใช้ร่วมกันได้ทั้งเลขบิลและเลข Lot)
- [ ] ออกแบบ Transaction + Lock สำหรับการสร้างบิล/จัดกลุ่ม Lot/ตัดสต็อก
- [ ] ทำ Audit log แบบ insert-only + hash-chaining
- [ ] ออกแบบ idempotency key ในทุก endpoint ที่สร้างบิล/สร้าง Lot
- [ ] ออกแบบข้อจำกัดฐานข้อมูลป้องกันรายการซื้อถูกจัดเข้า Lot ซ้ำ
- [ ] ล็อก Lot ที่ "ปิดแล้ว" ให้แก้ไขได้เฉพาะผ่านรายการปรับปรุง ไม่เขียนทับ
- [ ] กำหนดมาตรฐานการเก็บตัวเลขการเงิน/น้ำหนักให้แม่นยำ (หลีกเลี่ยงปัญหาปัดเศษสะสม)
- [ ] เพิ่ม JWT + RBAC และ rate limit บน endpoint login
- [ ] ตั้งระบบ backup อัตโนมัติไป R2 (แยกจาก Google Sheets) + ทำ restore drill
- [ ] ต่อ error monitoring (Sentry) และ log pipeline
- [ ] ย้าย secrets ทั้งหมดไป Cloudflare Workers Secrets
- [ ] ปรึกษาผู้ทำบัญชีเรื่อง e-Tax Invoice และข้อกำหนด VAT

---

*หมายเหตุ: เอกสารนี้เป็นคำแนะนำเชิงสถาปัตยกรรมทั่วไป ไม่ใช่คำแนะนำทางกฎหมายหรือบัญชี ควรตรวจสอบข้อกำหนดล่าสุดของกรมสรรพากรและปรึกษาผู้เชี่ยวชาญด้านบัญชี/ภาษีก่อนนำไปใช้งานจริง*
